on: [push, pull_request]

name: Test-Suite nightly macos-14

jobs:
  CI-nightly-macos-14:
    uses: ./.github/workflows/reuse.yml
    with:
      toolchain: nightly
      os: macos-14
      packages: vtk
      environment_vars: VTK_VERSION=-9.4
